<h2>$loggedInUser.getUsername()'s Account</h2>
#if($games.size() < 1)
  <h4>You have no saved games!</h4>
#else
  <h4>Saved Games:</h4>
  <ul>
    #foreach($game in $games)
      <li>Game: ID # $game.getId()</li>
    #end
  </ul>
#end

<form action="/logout" method="post">
    <button type="submit" class="btn btn-danger">Logout</button>
</form>
